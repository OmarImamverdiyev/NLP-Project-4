# UI Artifacts

This directory is reserved for UI-side cached assets.

Expected contents:

- `model_store/`: locally saved Hugging Face bundles reused by the dashboard and future runs
- additional logs or exported run metadata if needed later

The heavy cache folders are ignored by Git.
